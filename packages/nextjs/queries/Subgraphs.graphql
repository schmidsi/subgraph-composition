query Subgraphs {
  crossSubgraphs(first: 10, orderBy: currentSignalledTokens, orderDirection: desc, where: { entityVersion: 2 }) {
    id
    deployedChain
    metadata {
      displayName
    }
  }
}
